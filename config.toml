# Scorpio Analyst — default configuration
# Override with .env or environment variables (prefix: SCORPIO_)

[llm]
default_provider = "openai"
quick_thinking_model = "gpt-4o-mini"
deep_thinking_model = "o3"
max_debate_rounds = 3
max_risk_rounds = 2
agent_timeout_secs = 30

[trading]
asset_symbol = "AAPL"
backtest_start = "2024-06-01"
backtest_end = "2024-11-30"

[api]
finnhub_rate_limit = 30
